(()=>{
if(window.__SETARI_AUTHOR_MESSAGES_FIX__)return;window.__SETARI_AUTHOR_MESSAGES_FIX__=true;
const PROJECT='iqfrakjlabkjxygdiktp',KEY='sb_publishable_kntNAWBHQRobiuQmh2xStA_BnCnz0p1',BASE='https://iqfrakjlabkjxygdiktp.supabase.co/rest/v1/';
const lang=()=>localStorage.getItem('setariLang')==='en'?'en':'pt',t=(pt,en)=>lang()==='en'?en:pt;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmt=d=>{if(!d)return '—';try{return new Intl.DateTimeFormat(lang()==='en'?'en-US':'pt-BR',{dateStyle:'short',timeStyle:'short'}).format(new Date(d))}catch{return d}};
function token(){for(let i=0;i<localStorage.length;i++){const k=localStorage.key(i)||'';if(!k.includes(PROJECT)||!k.includes('auth-token'))continue;try{const v=JSON.parse(localStorage.getItem(k)||'{}');return v?.access_token||v?.currentSession?.access_token||v?.session?.access_token||null}catch{}}return null}
function uid(){try{const p=(token()||'').split('.')[1];if(!p)return null;const s=p.replace(/-/g,'+').replace(/_/g,'/').padEnd(Math.ceil(p.length/4)*4,'=');return JSON.parse(atob(s)).sub||null}catch{return null}}
async function api(path,opt={}){const tk=token();if(!tk)throw new Error(t('Sessão não encontrada.','Session not found.'));const r=await fetch(BASE+path,{...opt,headers:{apikey:KEY,Authorization:`Bearer ${tk}`,'Content-Type':'application/json',...(opt.headers||{})}});const d=await r.json().catch(()=>null);if(!r.ok)throw new Error(d?.message||d?.details||d?.hint||`HTTP ${r.status}`);return d}
const decision=v=>({revision_requested:t('Revisão solicitada','Revision requested'),accepted:t('Aceito','Accepted'),rejected:t('Rejeitado','Rejected')}[v]||t('Comunicação editorial','Editorial communication'));
function styles(){if(document.getElementById('setari-author-msg-fix-style'))return;const s=document.createElement('style');s.id='setari-author-msg-fix-style';s.textContent=`#setari-author-messages{margin:1.2rem 0;border:2px solid #0a5961;background:#fbfefe}.amf-card{border:1px solid #d5e4e3;border-radius:10px;background:#fff;padding:1rem;margin:.8rem 0}.amf-head{display:flex;justify-content:space-between;gap:1rem;align-items:flex-start;flex-wrap:wrap}.amf-chip{display:inline-block;border-radius:999px;padding:.25rem .5rem;background:#dff2f0;color:#075761;font-size:.72rem;font-weight:900}.amf-message{white-space:pre-wrap;background:#f4f9f9;border-radius:8px;padding:.9rem;margin-top:.6rem;line-height:1.5}.amf-reviews{margin-top:.9rem;padding-top:.8rem;border-top:1px solid #e1e9e8}.amf-empty{padding:1rem;border:1px dashed #b8cecc;border-radius:9px;background:#f8fbfb}`;document.head.appendChild(s)}
function panel(){const parent=document.getElementById('author-panel');if(!parent||parent.hidden)return null;styles();let p=document.getElementById('setari-author-messages');if(!p){p=document.createElement('section');p.id='setari-author-messages';p.className='panel';parent.prepend(p)}p.innerHTML=`<span class="kicker">${t('Comunicação da Editoria','Editorial communication')}</span><h2>${t('Mensagens da Editoria','Editorial Messages')}</h2><p>${t('Aqui aparecem as decisões e mensagens enviadas pelo Editor-Chefe, juntamente com os pareceres liberados para o autor.','Editorial decisions and messages from the Editor-in-Chief appear here, together with reviews released to the author.')}</p><div id="amf-content"><div class="amf-empty">${t('Carregando…','Loading…')}</div></div>`;return p}
let loading=false,lastUser='';
async function load(){const p=panel();if(!p||loading)return;const me=uid();if(!me)return;loading=true;const box=p.querySelector('#amf-content');try{const [subs,msgs]=await Promise.all([api(`submissions?select=id,code,title,status&author_id=eq.${encodeURIComponent(me)}&order=submitted_at.desc`),api(`editorial_messages?select=id,submission_id,message,decision,created_at&author_id=eq.${encodeURIComponent(me)}&order=created_at.desc`)]);box.innerHTML='';if(!msgs?.length){box.innerHTML=`<div class="amf-empty">${t('Nenhum retorno editorial recebido até o momento.','No editorial return has been received yet.')}</div>`;return}for(const s of subs||[]){const sm=msgs.filter(m=>m.submission_id===s.id);if(!sm.length)continue;let reviews=[];try{reviews=await api('rpc/author_review_feedback',{method:'POST',body:JSON.stringify({p_submission_id:s.id})})||[]}catch{}const c=document.createElement('article');c.className='amf-card';c.innerHTML=`<div class="amf-head"><div><strong>${esc(s.code||'')} · ${esc(s.title)}</strong></div><span class="amf-chip">${esc(s.status||'')}</span></div>${sm.map(m=>`<div class="amf-message"><span class="amf-chip">${esc(decision(m.decision))}</span> <span class="fine">${fmt(m.created_at)}</span><br><br>${esc(m.message)}</div>`).join('')}${reviews.length?`<div class="amf-reviews"><strong>${t('Pareceres liberados pela Editoria','Reviews released by the Editorial Office')}</strong>${reviews.map((r,i)=>`<div class="amf-message"><b>${t('Parecer','Review')} ${i+1}</b> · ${esc(r.recommendation||'—')}<br><br>${esc(r.comments_to_author||'—')}</div>`).join('')}</div>`:''}`;box.appendChild(c)}if(!box.children.length)box.innerHTML=`<div class="amf-empty">${t('Nenhum retorno editorial recebido até o momento.','No editorial return has been received yet.')}</div>`}catch(e){box.innerHTML=`<div class="amf-empty">${esc(t('Não foi possível carregar as mensagens: ','Could not load messages: ')+e.message)}</div>`}finally{loading=false}}
function detect(){const ap=document.getElementById('author-panel'),me=uid();if(ap&&!ap.hidden&&me){if(me!==lastUser||!document.getElementById('setari-author-messages')){lastUser=me;load()}}else if(!me)lastUser=''}
function boot(){detect();setInterval(detect,1000);document.addEventListener('submit',e=>{if(e.target?.id==='login-form')setTimeout(()=>{lastUser='';detect()},1200)},true);document.addEventListener('visibilitychange',()=>{if(!document.hidden){lastUser='';detect()}});window.addEventListener('focus',()=>{lastUser='';detect()});window.addEventListener('setari:lang',()=>{lastUser='';detect()})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();